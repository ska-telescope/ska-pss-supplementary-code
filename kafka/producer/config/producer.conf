# AT4-2180 standalone Kafka producer — default configuration.
bootstrap.servers = localhost:9092
topic             = pss-single-pulse-candidates

producer_id       = pss-test-node-01

acks              = all
retries           = 5
linger.ms         = 5
compression.type  = none
message.max.bytes = 4194304

# Synthetic payload (test-only; replaced once real .fil reader lands)
synthetic.payload_bytes        = 2300000
synthetic.scheduling_block_id  = sbi-test-0001
synthetic.beam_id              = beam-000
